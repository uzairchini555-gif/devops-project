apiVersion: v1
kind: Service
metadata:
  name: project-service
spec: 
  type: NodePort
  selector: 
    app: project-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007

