name: Deploy to EKS 
on: 
  push: 
    branches: 
    - main 
jobs:   
  deploy: 
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code 
      uses: actions/checkout@v3
    - name: Login to Docker Hub 
      uses: docker/login-action@v2
      with: 
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and Push Docker Image 
      run: |
         docker build -t ${{ secrets.DOCKER_USERNAME }}/project-app:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/project-app:latest
    - name: Configure AWS Credentials 
      uses: aws-actions/configure-aws-credentials@v2
      with: 
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: $[[ secrets.AWS_SECRET_KEY }}
        aws-region: us-east-1
    - name: Update Kubeconfig
      run: |
        aws eks update-kubeconfig --region us-east-1 --name uzair-eks-cluster 
    - name: Deploy to Kubernetes
      run: |
        kubectl tapply -f deployment.yaml
        kubectl apply -f service.yaml
        kubectl rollout restart deployment app-deployment 
